<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             x:Class="OfficeReservations.Views.CalendarPage"
             Title="Wybór terminu">

    <VerticalStackLayout Padding="20"
                         Spacing="16">

        <Label Text="Wybierz datę wizyty"
               FontSize="18"
               TextColor="Gray"/>

        <!-- Date picker -->
        <DatePicker Date="{Binding SelectedDate}"
                    MinimumDate="{x:Static sys:DateTime.Today}"
                    Format="dd MMMM yyyy"/>

        <!-- Available slots -->
        <Label Text="Dostępne godziny"
               FontSize="16"
               FontAttributes="Bold"
               Margin="0,8,0,0"/>

        <Label Text="Brak dostępnych terminów w tym dniu"
               TextColor="Gray"
               IsVisible="{Binding NoSlotsAvailable}"/>

        <CollectionView ItemsSource="{Binding AvailableSlots}"
                        SelectionMode="Single"
                        SelectedItem="{Binding SelectedSlot}"
                        HeightRequest="300">
            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical"
                                 Span="4"
                                 HorizontalItemSpacing="8"
                                 VerticalItemSpacing="8"/>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Border Stroke="#bdc3c7"
                            StrokeThickness="1"
                            Padding="8,6"
                            HeightRequest="40">
                        <Label Text="{Binding ., StringFormat='{0:HH\\:mm}'}"
                               HorizontalOptions="Center"
                               FontSize="14"/>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Text="Dalej"
                Command="{Binding ProceedCommand}"
                HorizontalOptions="Fill"
                Margin="0,8,0,0"/>

    </VerticalStackLayout>
</ContentPage>